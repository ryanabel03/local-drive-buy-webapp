!!! 5
%html
  %head
    = render partial: "shared/header"
  %body
    .navbar
      .navbar-inner
        %a.brand{href: "#"}
        %ul.nav
          = nav_link "Listings", href: listings_path, tab: :listings
    - if notice.present?
      %p{:class => "notice"}= notice
    - if alert.present?
      %p{:class => "alert"}= alert
    = yield
